<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Trade Bitcoin</title>
</head>
<body>

<div class="tradebitcoin container">
    <div class="col-md-12 text-center main">
      <p>
        <%= image_tag "bitcoin.jpg" %>
      </p>
      <h3>BitCoinTrader</h3>

        <p>Compre e venda bitcoins na maior corretora do Brasil!!</p>

      <div class="col-md-12">

        <div class="col-md-6 login-box">
          <p>Para começar, faça seu <%= link_to 'login', user_session_path %></p>


          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="field">
              E-mail<br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
            </div>

            <div class="field">
              Senha<br />
              <%= f.password_field :password, autocomplete: "off" %>
            </div>

            <% if devise_mapping.rememberable? -%>
              <div class="field">
                <%= f.check_box :remember_me %>
                Lembrar-se de mim
              </div>
            <% end -%>

            <div class="actions">
              <%= f.submit "Entrar" %>
            </div>
          <% end %>

          <%= render "devise/shared/links" %>

          <%#= link_to 'Cadastrar', 'users/new' %>

        </div>

      </div>

    </div>

</div>

</body>
</html>